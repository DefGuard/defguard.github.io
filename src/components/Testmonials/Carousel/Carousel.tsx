import React, { useState, useEffect } from 'preact';
import styles from './style.scss';
import { getCollection } from 'astro:content';

const Carousel = () => {
    const [testmonials, setTestmonials] = useState();

    useEffect(() => {

    });

};

export default Carousel;