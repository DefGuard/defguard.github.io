---
import { DefguardIcon } from "../base/icons/DefguardIcon";

// const data = await fetch('https://api.github.com/repos/defguard/client/releases').then((response) =>
//   response.json()
// ).catch(error => {
//   console.log("Error" + error);
// });
// const latestVersion = data[0].name.slice(1);
// // 2024-07-08T12:58:59Z -> 08.07.2024
// const published = data[0].published_at.split('T')[0].split('-').reverse().join('.');
---


<!-- TODO(cpprian): write react component and while waiting add null as a text -->
<section class="download-version">
  <div class="download-logo">
    <DefguardIcon client:only />
  </div>
  <div class="download-version-info">
    <div class="download-version-current">
      <p>Current version:</p>
      <!-- <p>{latestVersion}</p> -->
       <p>0.4.0</p>
    </div>
    <div class="download-changelog">
      <a href="https://github.com/DefGuard/client/releases">View changelog →</a>
      <!-- <p>{published}</p> -->
      <p>16.07.2024</p>
    </div>
  </div>
</section>

<style lang="scss" is:global>
  #download-page {
    .download-version {
      display: flex;
      width: 100%;
      max-width: 382px;
      flex-direction: column;
      justify-content: space-between;
      align-items: flex-start;
      align-self: stretch;

      .download-logo {
        display: flex;
        // height: 382px;
        padding: 47.75px 120.262px 47.75px 122.359px;
        justify-content: center;
        align-items: center;
        border-radius: 30px;
        background: #0c8ce0;
      }

      .download-version-info {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: flex-start;
        gap: 8px;
        align-self: stretch;

        .download-version-current,
        .download-changelog {
          display: flex;
          justify-content: space-between;
          align-items: flex-start;
          align-self: stretch;
        }

        .download-version-current {
          @include typography(pricebox-price);
        }

        .download-changelog {
          @include typography(pricebox-list);
        }
      }
    }

    @include break-down(sm) {
      .download-version {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 40px;
        align-self: stretch;

        .download-logo {
          background: none;
        }
      }
    }
  }
</style>
